local g = import 'base.libsonnet';
local m = import 'modifiers.libsonnet';

local normal_ = g.generateUnicodeArray("A", g.LATIN_ALPHABET_LENGTH);
local new_ = g.generateUnicodeArray("Á", g.LATIN_ALPHABET_LENGTH);


local rawMatches = [
    {
        trigger: normal_[i],
        replace: new_[i],
    },
    for i in std.range(0, std.length(normal_) - 1)
];

{
    normal: normal_,
    new: new_,
    matches: rawMatches,

}